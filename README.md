# HateLens
### A Trust & Safety-Oriented Binary Hate Speech Classifier
### with Human-in-the-Loop Policy Annotation

**Built as a portfolio project to demonstrate content moderation ML workflows
relevant to Trust & Safety operations.**

---

## What This Project Does

HateLens trains a binary text classifier to identify hate speech in social media posts.
Unlike a standard ML project, it is designed around the actual workflow of a Trust & Safety team:

- Identifies an **"uncertain zone"** of tweets for human review rather than treating all predictions equally
- Justifies its threshold choice in plain English (see [POLICY_RATIONALE.md](POLICY_RATIONALE.md))
- Documents the human review process as a formal **Policy Decision Log** with 100 completed annotations
- Includes a written analysis of the classifier's known limitations for a non-technical audience
- Every code cell has a WHY comment explaining the reasoning, not just the mechanics

---

## Dataset

**Davidson et al. (2017)** — 24,783 labeled tweets  
Source: https://github.com/t-davidson/hate-speech-and-offensive-language  
Original labels: 3-class (hate speech / offensive language / neither)  
This project: Collapsed to binary (hate vs. not hate)

**Actual class ratio: 16.33:1** (not hate : hate) — hate speech is only 5.8% of the dataset.

> **Why binary?** A content moderator makes a binary decision — remove or keep.
> "Offensive but not hate" and "neutral" are collapsed to the same output class
> because this classifier's job is to surface the final removal decision.

> **Why 16:1 and not 7:1?** Most papers report the 3-class distribution. When collapsing
> to binary — mapping both "offensive" and "neither" to label=0 — only the 1,430 rows
> labelled as hate speech (class=0) become label=1. This produces a 16.33:1 imbalance,
> which is more severe than the figure commonly cited.

---

## Results

| Model | Accuracy | Hate F1 | Hate Recall | Hate Precision |
|-------|----------|---------|-------------|----------------|
| Baseline (no imbalance fix) | 0.9447 | 0.1893 | 0.1119 | 0.6154 |
| + SMOTE | 0.8943 | 0.3907 | 0.5874 | 0.2927 |
| + Class Weights (balanced) | 0.8953 | 0.4082 | 0.6259 | 0.3029 |
| **+ Class Weights + Threshold=0.35** | **0.8301** | **0.3391** | **0.7552** | **0.2186** |

**Final model: Class Weights + Threshold = 0.35**

Note: The final model has a lower F1 than the Class Weights model at threshold=0.50.
This is intentional — see "Why This Threshold?" below.

---

## Why This Threshold?

See [POLICY_RATIONALE.md](POLICY_RATIONALE.md) for the full explanation.

**Short answer:** The F1-maximizing threshold is 0.50 (F1=0.408, Recall=0.626).
We chose 0.35 instead (F1=0.339, Recall=0.755) because:

1. This system does **not auto-remove**. Every flagged post goes to a human reviewer first.
2. At threshold=0.35, we catch **37 more hate speech posts** on the test set (216 vs. 179).
3. Those 37 posts would otherwise stay on the platform undetected.

Missing a hate speech post harms a real targeted user. Over-flagging a legitimate post costs
reviewer time. The asymmetry in consequences — not F1 optimization — drives the threshold choice.

---

## Project Structure

```
HateLens/
│
├── README.md                               <- This file
├── POLICY_RATIONALE.md                     <- Non-technical threshold justification
├── requirements.txt
├── .gitignore
│
├── notebooks/
│   └── HateLens_Full_Pipeline.ipynb        <- Complete pipeline, all 10 sections, outputs included
│
├── data/
│   ├── labeled_data.csv                    <- Download from Davidson repo (not committed)
│   ├── binary_labeled_data.csv             <- Generated by notebook Section 2
│   ├── policy_decision_log_template.csv    <- 500-row grey zone annotation template
│   └── policy_decision_log_filled.csv      <- 100 completed human annotations
│
├── src/
│   ├── preprocess.py                       <- Canonical preprocessing function (imported by notebook)
│   ├── train.py                            <- CLI retraining script
│   └── evaluate.py                         <- Evaluation and plotting functions
│
└── outputs/
    ├── class_distribution.png
    ├── confusion_matrix_baseline.png
    ├── confusion_matrix_final.png          <- Threshold=0.35
    ├── precision_recall_curve.png
    ├── model_comparison_chart.png          <- All 4 model variants
    ├── grey_zone_categories.png            <- Annotation category breakdown
    ├── model.joblib                        <- Trained LogisticRegression model
    └── vectorizer.joblib                   <- Fitted TF-IDF vectorizer
```

---

## How to Run

### 1. Clone the repo

```bash
git clone https://github.com/YOUR_USERNAME/HateLens.git
cd HateLens
```

### 2. Install dependencies

```bash
pip install -r requirements.txt
```

### 3. Download the dataset

`labeled_data.csv` is not committed to this repo because it is not ours to redistribute.  
Download it from: https://github.com/t-davidson/hate-speech-and-offensive-language/tree/master/data  
Place it at: `data/labeled_data.csv`

### 4. Run the notebook

```bash
jupyter notebook notebooks/HateLens_Full_Pipeline.ipynb
```

Use **Kernel → Restart & Run All**. Section 10 (final cell) prints all result numbers.  
Expected runtime: 3–5 minutes (preprocessing takes ~60–90 seconds).

### 5. Retrain from the CLI

```bash
python src/train.py \
    --data data/binary_labeled_data.csv \
    --model_out outputs/model.joblib \
    --vectorizer_out outputs/vectorizer.joblib
```

### 6. Smoke test the saved model

```python
import joblib, sys, re
sys.path.insert(0, 'src')
from preprocess import preprocess_tweet

model = joblib.load('outputs/model.joblib')
vectorizer = joblib.load('outputs/vectorizer.joblib')

tweets = [
    "I love how diverse our community is becoming",
    "People from that country are all criminals and should be deported",
    "Go back to where you came from, you don't belong here",
]

for tweet in tweets:
    vec = vectorizer.transform([preprocess_tweet(tweet)])
    prob = model.predict_proba(vec)[0][1]
    label = 'HATE' if prob >= 0.35 else 'NOT HATE'
    print(f"{label} ({prob:.3f}): {tweet}")
```

---

## Grey Zone Annotation

After identifying the grey zone (630 tweets where predicted probability fell between 0.35–0.65),
**100 tweets were manually reviewed** using the annotation protocol in Section 9 of the notebook,
following T&S session structure (max 50 tweets per session, 15-minute break between sessions).

**Results:**
- 9 labeled as hate speech (9.0%), 91 labeled as not hate speech
- 14 disagreements with the original Davidson dataset labels — confirming the dataset's
  documented bias of over-labeling AAVE as hate speech
- 2 low-confidence decisions (genuinely ambiguous; would escalate to senior reviewer)

**Most common false positive pattern (model flagged, human labeled 0):**  
AAVE with reclaimed slurs — tweets where slur presence triggered the model, but speaker
identity and context indicated reclaimed in-group usage, not hate.

**Genuine violations found (9 total):**

| Category | Count |
|----------|-------|
| Race / ethnicity | 3 |
| Sexual orientation | 3 |
| Disability | 1 |
| Religion | 1 |
| National origin | 1 |

Files: `data/policy_decision_log_filled.csv` | `outputs/grey_zone_categories.png`

---

## Known Limitations

1. **Coded language failure.** TF-IDF cannot detect dog-whistle terms, coded references,
   or recently coined slang. These require continuous lexicon updates or a context-aware model.

2. **Context blindness.** "I hate racists" and "I hate [racial group]" score similarly.
   TF-IDF sees the word "hate" — it does not see intent.

3. **English-only.** Trained on English tweets. Not validated for multilingual content.

4. **Static training data.** The Davidson 2017 dataset is nearly a decade old.
   Hate speech vocabulary has evolved substantially. Regular retraining is required.

5. **Platform-specificity.** Trained on Twitter data. Performance on Reddit, YouTube,
   WhatsApp, or Discord is not validated.

6. **Severe imbalance ceiling.** At 16.33:1 imbalance with TF-IDF features, the model
   achieves F1=0.34 on the hate class even with class weighting and threshold tuning.
   This is a hard architectural ceiling, not a tuning problem.

---

## What a Production Version Would Look Like

1. Fine-tune **HateBERT** (BERT pre-trained on banned Reddit content) — expected F1 > 0.70
2. Add **character-level features** to catch intentional misspellings ("h4te", "k!ll")
3. Build an **active learning pipeline** where human reviewer decisions continuously retrain the model
4. Deploy **drift monitoring** to detect vocabulary shift from the training distribution
5. Implement a **reviewer wellbeing program** — content moderation causes documented psychological harm

---

## Honest Assessment

This is a portfolio demonstration. The classifier is functional and the methodology is sound.
It should not be deployed on a real platform for three reasons:

1. The training data is from 2017 — hate speech vocabulary has changed substantially
2. TF-IDF + Logistic Regression is outperformed by transformer models by a wide margin on this task
3. Grey zone annotation covers 100 of 500 identified uncertain tweets — a genuine sample,
   but inter-rater agreement was not measured (single annotator)

These limitations are documented explicitly because Trust & Safety work requires
intellectual honesty about what a system can and cannot do.

---

*Dataset credit: Davidson, T., Warmsley, D., Macy, M. & Weber, I. (2017).
"Automated Hate Speech Detection and the Problem of Offensive Language."
Proceedings of the 11th AAAI International Conference on Web and Social Media.*
